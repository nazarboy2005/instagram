providers = ["php"]

[phases.setup]
nixPkgs = ["php82", "php82Extensions.curl", "php82Extensions.mbstring"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t 'Web Server Files' 'Web Server Files/router.php'"
