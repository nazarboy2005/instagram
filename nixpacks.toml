[phases.setup]
nixPkgs = ["php82", "php82Extensions.curl", "php82Extensions.mbstring"]

[phases.install]
cmds = ["echo 'PHP environment ready'"]

[start]
cmd = "cd 'Web Server Files' && php -S 0.0.0.0:${PORT:-8080} router.php"
